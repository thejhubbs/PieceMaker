<% user ||= @user %>
<% set_remote ||= !controller.request.format.html? %>

<%= form_for(user, :remote => set_remote) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, :class => 'form-control' %>
  </div>
  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email, :class => 'form-control' %>
  </div>
  <div class="field">
    <%= f.label :bio %>
    <%= f.text_field :bio, :class => 'form-control' %>
  </div>
  <div class="field">
    <%= f.label :account_id %>
    <%= f.text_field :account_id, :class => 'form-control' %>
  </div>
  <div class="field">
    <%= f.label :status %>
    <%= f.text_field :status, :class => 'form-control' %>
  </div>
  <div class="actions">
    <%= f.submit 'Save', :class => 'form-control'%>
  </div>
<% end %>
