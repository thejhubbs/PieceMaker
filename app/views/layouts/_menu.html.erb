<%= menu(image_tag(@site_layout_options.logo) + ' ' + @site_layout_options.title) do %>
  <% if true %>
  <% p = Clientalk::Page.where(:name => 'For Stores').first %>
  <li><%= link_to p.name, page_path(p.name.downcase.parameterize) %></li>
  <% p = Clientalk::Page.where(:name => 'For Consumers').first %>
  <li><%= link_to p.name, page_path(p.name.downcase.parameterize) %></li>
  <% p = Clientalk::Page.where(:name => 'Toy Designers').first %>
  <li><%= link_to p.name, page_path(p.name.downcase.parameterize) %></li>
  <% p = Clientalk::Page.where(:name => 'About Us').first %>
  <li><%= link_to p.name, page_path(p.name.downcase.parameterize) %></li>
  <% end %>
<% end %>

  
